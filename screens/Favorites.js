import { Text } from "react-native";

// local imports
import SafeView from "../components/SafeView";

const Favorites = () => {
  return (
    <SafeView>
      <Text>Favorites</Text>
    </SafeView>
  );
};

export default Favorites;
